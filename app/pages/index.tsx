import React from 'react';

export default function Index() {
  return (
    <div>
        <a href={`${process.env.NEXT_PUBLIC_BASE_PATH}/Testing`}>Go to Testing Page</a>        
        <h1>HELLO WORLD LINKS WORLS HOPE SO</h1>
    </div>
  );
}